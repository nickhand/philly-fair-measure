"""Model layer: baseline valuation models and evaluation (Milestone 6).

Import-light on purpose: the serve image installs only the API dependencies
(requirements.serve.txt) and imports submodules of this package
(``models.comps``, ``models.scoring``), so nothing here may pull in the
training stack at import time.
"""
